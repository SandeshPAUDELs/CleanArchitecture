import 'package:flutter/material.dart';

class LendersSearch extends StatelessWidget {
  const LendersSearch({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}