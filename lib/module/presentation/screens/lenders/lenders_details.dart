import 'package:flutter/material.dart';

class LendersDetails extends StatelessWidget {
  const LendersDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}